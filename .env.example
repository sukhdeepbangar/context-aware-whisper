# Transcription Backend
# Options: "groq" (cloud, requires API key) or "local" (whisper.cpp, offline)
# CAW_TRANSCRIBER=groq

# Required when CAW_TRANSCRIBER=groq: Your Groq API key
GROQ_API_KEY=your_api_key_here

# Local Transcription Settings (only used when CAW_TRANSCRIBER=local)
# Model options: tiny.en, base.en, small.en, medium.en, large-v1, large-v2, large-v3
# CAW_WHISPER_MODEL=base.en
# CAW_MODELS_DIR=~/.cache/whisper

# Optional: Language code for transcription (auto-detected if not set)
# CAW_LANGUAGE=en

# Optional: Delay between keystrokes in seconds (0 = fastest)
# CAW_TYPE_DELAY=0

# Optional: Audio sample rate in Hz (default 16000)
# CAW_SAMPLE_RATE=16000

# Optional: Use clipboard paste instead of keystrokes (default false)
# CAW_USE_PASTE=false

# Text Cleanup Settings
# Removes speech disfluencies (um, uh, false starts, etc.)
# Options: off, light, standard, aggressive
# CAW_TEXT_CLEANUP=standard

# Preserve intentional patterns like "I like pizza" vs filler "like"
# CAW_PRESERVE_INTENTIONAL=true

# Local model for AGGRESSIVE mode (MLX model from HuggingFace, Apple Silicon only)
# Requires: pip install 'context-aware-whisper[local-llm]'
# Default: mlx-community/Phi-3-mini-4k-instruct-4bit (~2GB)
# Alternative: mlx-community/Mistral-7B-Instruct-v0.3-4bit (~4GB, better quality)
# CAW_LOCAL_MODEL=mlx-community/Phi-3-mini-4k-instruct-4bit
